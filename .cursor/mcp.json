{
  "mcpServers": {
    "mintlify": {
      "url": "https://mcp.mintlify.com"
    },
    "linear": {
      "command": "npx",
      "args": ["-y", "@linear/mcp"],
      "env": {
        "LINEAR_API_KEY": "${env:LINEAR_API_KEY}"
      }
    }
  }
}
